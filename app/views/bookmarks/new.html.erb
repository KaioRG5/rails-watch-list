<h1> Add a new Movie to <%= @list.name %></h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%# f.input :title %>
  <%= f.input :comment %>
  <%= f.input :movie_id, collection: @titles %>
  <%= f.submit 'Add Movie' %>
<% end %>




<%= link_to 'Go back', to: list_path(@list) %>
